services:
  db:
    ports:
      - "5432:5432"

  backend:
    command: sh -c "sleep 15 && python manage.py runserver 0.0.0.0:8000"
    volumes:
      - ../mineral-shop-backend:/app
    ports:
      - "8000:8000"
    stdin_open: true
    tty: true
